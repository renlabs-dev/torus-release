# Torus Network Release

With the `communex` Python library installed, we can extract the bridged
balances from the Commune chain and generate a JSON file with them.

```sh
python3 scripts/extract_genesis_balances.py
```
